An Application simulates a vending machine. 
It keeps track of items in the inventory, counts change, writes and reads to/from a file.

The application includes:
MVC architecture, including the use of a service layer
Spring Dependency Injection 
Spring AOP}
